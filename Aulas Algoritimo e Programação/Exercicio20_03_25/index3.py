# 3. Desenhar uma estrela de 5 pontas escrevendo manualmente cada movimento.

import turtle as pat
# pat = turtle.Turtle()
pat.shape("turtle")
pat.speed(5)

pat.forward(200)
pat.back(400)
pat.right(30)
pat.forward(400)
pat.left(135)
pat.forward(400)
pat.left(135)
pat.forward(400)
pat.left(145)
pat.forward(400)
pat.mainloop()

